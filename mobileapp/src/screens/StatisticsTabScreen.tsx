import React, {useEffect, useRef, useState} from 'react';
import {
  StyleSheet,
  View,
  Animated,
  Dimensions,
  Platform,
  TouchableOpacity,
  ActivityIndicator,
} from 'react-native';



const StatisticsTabScreen = ({navigation}: {navigation: any}) => {
  

  return (
    <View>
     
    </View>
  );
};

export default StatisticsTabScreen;

